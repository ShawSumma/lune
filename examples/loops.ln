# counter
var i : int = 0

#|
 While loop example
|#
proc whileloop() {
    while i < 100 {
        write("while loop")
        i = i + 1
    }
}

#|
 For loop example 
|#
proc forloop() {
    for i = 0, 10 {
        write("for loop")
        # no need to increment in for loop
    }
}