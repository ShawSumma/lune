# variables in lune
var intvar : int = 0

# strings are just char[]
var strvar : str = "hello world"

var charvar : char = 'a'

# pointers will be added to support C interoperability
var ptrvar : ptr int = addr(0)